{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "apt-get update && apt-get install -y texlive-latex-base"
    },
    "deploy": {
      "startCommand": "hypercorn main:app --bind \"[::]:$PORT\""
    }
  }